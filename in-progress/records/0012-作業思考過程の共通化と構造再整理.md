# 設計判断ログ

## 0012: 作業・思考過程の共通化、及び構造の再整理

- 日付: 2026-04-27
- 状態: 議論中
- ドキュメント: 未着手
- 実装: 未着手


### 背景

rec-0010 で整理した「やりたいこと」の2つ目。Claude Code との作業プロセス（採用ループ、records-add、critique/propose）をこのリポジトリだけでなく、展開先プロジェクトでも同じように使えるようにするための不足・ズレを明らかにする。

### 現状の構造（スキル配置）

```
~/.claude/skills/          ← グローバル（全プロジェクト共通）
  imported/                ← （中身不明）

.claude/skills/            ← このリポジトリローカル
  critique/
  monorepo-init/
  package-add/
  propose/
  records-add/
```

### 指摘

- スキルはグローバルに存在するため「どのプロジェクトでも呼べる」は達成済み
- しかし展開先の `CLAUDE.md` にスキル一覧・使い方が書かれていない → 展開先の Claude と人間がスキルの存在を知れない
- `critique` / `propose` / `records-add` の使い方（採用ループ）が CLAUDE.md にあるのはこのリポジトリのみ
- `monorepo-init` / `package-add` は `.claude/skills/`（ローカル）にあり、グローバルに昇格していない

### 判断

（未定）

### 理由

（未定）

### 却下した選択肢

（未定）

### 影響

（未定）
